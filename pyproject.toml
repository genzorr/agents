[project]
name = "agents"
version = "0.1.0"
description = "Personal/global shared Agents, Codex, Claude, and Devin asset source and install surface."
requires-python = ">=3.9"
dependencies = []

[tool.uv]
package = false
