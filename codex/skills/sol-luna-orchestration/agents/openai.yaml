interface:
  display_name: "Sol-Luna Orchestration"
  short_description: "Configure Luna/xhigh work and escalated Sol/high review."
  default_prompt: "Use $sol-luna-orchestration to configure Sol-led work with reusable Luna/xhigh subagents by default, reusable Sol/high review only for a named acceptance-critical target where independent judgment has material value, and an ordinary Luna task/thread only when I request one."
policy:
  allow_implicit_invocation: false
