interface:
  display_name: "Surface Unknowns"
  short_description: "Find blind spots and route material uncertainty."
  default_prompt: "Use $surface-unknowns to help me understand this problem, expose consequential unknowns, and recommend the next way to resolve them."

policy:
  allow_implicit_invocation: false
